import type { RadiusSearchEstablishment } from "../../types/api";
import { matchLabel } from "./discoveryUtils";
const show = (value: string | null) => value === null || value === "" ? "—" : value;
export function RadiusTable({ items, onSelect }: { items: RadiusSearchEstablishment[]; onSelect: (item: RadiusSearchEstablishment) => void }) {
  return <div className="table-scroll"><table className="results-table radius-table"><caption>Estabelecimentos retornados pela busca por raio</caption><thead><tr><th>Empresa</th><th>CNPJ</th><th>Município/UF</th><th>Distância</th><th>CNAE principal</th><th>Correspondência</th><th>Porte</th><th>Capital</th><th>Precisão</th><th>Situação comercial</th><th>Ação</th></tr></thead><tbody>{items.map((item) => <tr key={item.cnpj_full}><td><strong>{show(item.razao_social)}</strong><span className="cell-secondary">{show(item.nome_fantasia)}</span></td><td className="cell-code">{item.cnpj_full}</td><td>{[item.municipio_nome, item.uf].filter(Boolean).join(" / ") || "—"}</td><td>{item.distance_km.toLocaleString("pt-BR", { maximumFractionDigits: 2 })} km</td><td className="cell-code">{show(item.cnae_principal)}</td><td>{matchLabel(item)}</td><td>{show(item.porte_codigo)}</td><td>{show(item.capital_social)}</td><td>{item.location_precision}</td><td><span className="status-unknown">Desconhecido (provisório)</span></td><td><button className="table-action" type="button" onClick={() => onSelect(item)}>Ver detalhes</button></td></tr>)}</tbody></table></div>;
}
